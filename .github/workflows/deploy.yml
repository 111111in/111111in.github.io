name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build and Export
      run: npm run export
      env:
        NOTION_PAGE_ID: ${{ secrets.NOTION_PAGE_ID }}
        NEXT_PUBLIC_THEME: heo
        NEXT_PUBLIC_AUTHOR: auberginewly
        NEXT_PUBLIC_BIO: hi 我是一只🍆
        NEXT_PUBLIC_LINK: https://111111in.github.io
        NEXT_PUBLIC_BEI_AN: 赣ICP备2025066072号-2
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./out

